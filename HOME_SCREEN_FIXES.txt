# HOME_SCREEN FINAL FIXES

## Changes needed in lib/screens/home_screen.dart

### Fix 1: Add import at the top (after line 28)
```dart
import 'schedule_meeting_screen.dart';  // ADD THIS LINE
```

### Fix 2: Change the "Planifier" quick action (around line 288-295)
**FROM:**
```dart
Expanded(
  child: _QuickAction(
    icon: Icons.calendar_today_rounded,
    label: 'Planifier',
    color: const Color(0xFF7C3AED),
    onTap: () => Navigator.push(
      context,
      MaterialPageRoute(
        builder: (_) =>
            const CreateMeetingScreen(isScheduled: true),
      ),
    ),
  ),
),
```

**TO:**
```dart
Expanded(
  child: _QuickAction(
    icon: Icons.calendar_today_rounded,
    label: 'Planifier',
    color: const Color(0xFF7C3AED),
    onTap: () => Navigator.push(
      context,
      MaterialPageRoute(
        builder: (_) =>
            const ScheduleMeetingScreen(),
      ),
    ),
  ),
),
```

---

## Why these fixes:
1. **Import** — ScheduleMeetingScreen needs to be imported to be used
2. **Parameter fix** — CreateMeetingScreen doesn't have an `isScheduled` parameter. The new professional scheduler is `ScheduleMeetingScreen` which is the replacement for scheduled meetings
