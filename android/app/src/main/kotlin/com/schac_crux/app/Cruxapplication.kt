package com.schac_crux.app

import io.flutter.app.FlutterApplication

/**
 * Application de base CRUX.
 *
 * Créée pour satisfaire la référence `android:name=".CruxApplication"` du
 * manifest. Ne contient pour l'instant aucune logique propre — point
 * d'extension naturel pour une future initialisation globale (DataStore,
 * MLKit, etc.) si besoin.
 */
class CruxApplication : FlutterApplication()
