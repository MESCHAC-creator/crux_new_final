# LiveKit
LIVEKIT_API_KEY=your_livekit_api_key_here
LIVEKIT_API_SECRET=your_livekit_api_secret_here

# CORS (origines autorisées séparées par virgule)
ALLOWED_ORIGINS=https://crux-3c6be.web.app,http://localhost:3000

# Serveur
PORT=3000
TOKEN_TTL_SECONDS=3600

# Firebase Admin (en dev local uniquement — en prod : utiliser ADC ou service account)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccount.json

# ⚠️  NE JAMAIS mettre les clés PayDunya ici
# Utilisez : firebase functions:secrets:set PAYDUNYA_MASTER_KEY
#             firebase functions:secrets:set PAYDUNYA_PRIVATE_KEY
#             firebase functions:secrets:set PAYDUNYA_TOKEN
